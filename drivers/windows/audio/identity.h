// Identity of the Prukka Speaker (Windows): apps play the far end into
// its speaker endpoint and the engine captures it from the microphone
// side (call profile). Same PortCls loopback core as ../common.
#pragma once
#define PRUKKA_DRIVER_NAME L"Prukka Speaker"
