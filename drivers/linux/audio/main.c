// SPDX-License-Identifier: GPL-2.0-only
/*
 * Prukka Speaker — this module is its identity over the shared ALSA
 * loopback core; including the core here is the standard out-of-tree
 * kbuild way to build one source into differently-named modules.
 */
#include "../common/pcm_loop.c"
