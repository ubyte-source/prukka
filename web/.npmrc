# Block dependency lifecycle scripts during install. The dashboard build
# (svelte-check + vite) needs no dependency install scripts; fsevents' native
# binding is an optional dev-watch optimization only. Native npm enforces this,
# unlike the strict-allow-scripts key it silently ignores without a plugin.
ignore-scripts=true
strict-allow-scripts=true
